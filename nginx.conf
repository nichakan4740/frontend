events {
    worker_connection 1024;
}
http{
    include /etc/nginx/mime.type;
    sendfile on;
}
server{
    root /usr/share/nginx/html;

    location /api{
        proxy_pass http://202.44.9.103:8080/kw2/api;
         rewrite ^/api(.*)$ /$1;
    }
 }