upstrram backendServer{
   server backend:8080;
}
server {
   listen 80;
   listen [::]:80;
   location / {
      root /usr/share/nginx/html;
      index index.html index.html;
   }
   location /api {
      rewrite ^/api/(.*)$ /$1 break;
      proxy_pass http://backendServer;
   }
}



