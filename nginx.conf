events {
    worker_connection 1024;
}
http{
    include /etc/nginx/mime.type;
    sendfile on;
}
server{
    root /usr/share/nginx/html;

    location /api{
        proxy_pass backend;
         rewrite ^/api(.*)$ /$1;
    }
 }


