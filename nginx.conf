server {
   listen 8080;
   location / {
      root /usr/share/nginx/html;
      index index.html index.html;
   }
   location /api {
      proxy_pass backend:8080;
   }
    location /kw2/ {
      proxy_pass frontend:8080;
   }
}